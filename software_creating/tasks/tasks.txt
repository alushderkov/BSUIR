23. Вводится число N и массив размером N с клавы, вывести "+",
    если положительных чисел больше, чем отрицательных, "-",
    если наоборот, "=", если одинаковое количество

24. Задача на дублирование введённого символа в строке
    Было "ababc", символ 'а', итог "aabaabc"

25. Дан массив на N элементов, N - четное.
    Найти максимальную сумму пар элементов (пары по типу 1,2 элементы, 3,4 элементы, 5,6 элементы и т. д.)
    и количество пар с такой же суммой.
    Вывести на экран макс сумму и кол-во пар.
    Ввод пользователем через клавиатуру, правильность данных гарантируется.

26. Есть число n и массив размерностью n, проверить элементы массива
    на соответствие одной из двух последовательностей:
    x1<x2>x3<x4>x5 ...
    x1>x2<x3>x4<x5 ...
    Вывести наверн "yes", если соответствует хотя бы одной, и "no", если наоборот,
    сделать ещё считывание элементов массива с консоли (дефолт ввод с консоли по элементику массива)

27. Два числа, по ноду найти НОК по формуле НОК=а*(б/нод)
    Нод считать по алгоритму евклида

28. Есть строка, вводить как хочешь. Надо проверить закодирована ли она
    с помощью UTF-8. Размер символа от 1 до 4 байт. и там табличка с масками.

    1 байт: 0xxxxxxx
    2 байта: 110xxxxx 10xxxxxx
    3 байта: 1110xxxx 10xxxxxx 10xxxxxx
    4 байта: 11110xxx 10xxxxxx 10xxxxxx 10xxxxxx

    Где x произвольное

29. вводятся целочисленная переменная N и вещественный массив на N элементов. Если элементы в нем
    расположены строго по возрастанию или строго по убыванию, вывести YES, иначе вывести NO.
    про клаву или тип выделяемой памяти ничего не говорилось, поэтому я все статически объявлял

30. Вывести N элементов ряда Фибоначчи

31. Дан массив и число А. Число встречается несколько
    раз в массиве (может и 0, и 1 раз, и более раз).
    Найти самую последнюю позицию числа А в массиве (но индексация с 1).

32. Вводится целое число N, и массив из N вещественных чисел.
    Для всех элементов кроме первого сделать сдвиг влево
    (значение текущего должно быть присвоено значению предыдущего).
    Последний элемент должен быть равен первому
    И вывести массив. Для каждого элемента 7 позиций

33. Вводится с клавиатуры число N и целочисленный массив из N элементов.
    Преобразовать исходный массив следующим образом: находится максимальный
    элемент и ставится на первую позицию массива, далее из остальных элементов
    снова находится максимальный элемент и ставится на последнюю позицию в массиве,
    затем из оставшихся находим максимальный и ставит на вторую позицию, затем на
    предпоследнюю и так далее. Пример: (10, 11, 66, 91, 45) -> (91, 45, 10, 11, 66).
