## 🚀 Алгоритмические задачи и структуры данных

### 1. Остаток от деления n-го числа Фибоначчи

**Задача**  
Найти остаток от деления $n$-го числа Фибоначчи на $m$ с эффективностью расчета не более чем за 2 секунды. При больших значениях $n$ (до $10^{18}$) прямой расчет неэффективен. Чтобы уложиться в заданное время, алгоритм должен работать за $O(\log\ n)$ или быстрее. Для этого можно воспользоваться следующими методами: метод быстрого возведения в степень через матрицы, позволяющий вычислить $F(n)$ за $O(\log\ n)$ , и использование периода Пизано, длина которого не превышает $m^2$, что позволяет вычислить его за $O(m^2)$, а затем сократить $n$ по модулю этого периода, ускоряя вычисление. Это позволяет значительно сократить время работы алгоритма и уложиться в заданные 2 секунды. Например, для входных данных `10 2` результатом будет `1`, так как десятое число Фибоначчи — 55, а 55 $mod$ 2 = 1.

### 2. Дробный рюкзак

**Задача**  
Найти максимальную стоимость набора предметов, который можно уместить в рюкзак заданного объема, с учетом возможности делить предметы на части. Дан рюкзак вместимостью $W$ и $n$ предметов, каждый из которых имеет массу $w_i$ и стоимость $v_i$. Разрешается брать дробную часть предмета, но нельзя превышать общий объем рюкзака. Для оптимального решения используется жадный алгоритм: предметы сортируются по убыванию стоимости на единицу массы $v_i/w_i$ , затем добавляются в рюкзак в порядке убывания этого показателя, пока есть свободное место. Сложность алгоритма составляет $` O(n\ \log\ n) `$ за счет сортировки. Например, если рюкзак вмещает `50`, а есть предметы с параметрами (масса, стоимость): `10 60`, `20 100`, `30 120`, то оптимальный выбор — взять полностью первые два предмета и 2/3 третьего, получив максимальную стоимость `240`.


### 3. Max-куча на основе массива

**Задача**  
Реализовать max-кучу на основе массива с операциями вставки элемента и извлечения максимального значения, используя только встроенные структуры данных JavaScript. Max-куча — это двоичная куча, в которой каждый родительский узел имеет значение больше или равное значениям своих потомков. Реализация должна включать методы `insert(value)`, добавляющий элемент в кучу с последующим восстановлением свойств кучи, и `extractMax()`, удаляющий и возвращающий максимальный элемент, перестраивая кучу после удаления. Операции должны работать за $O(\log\ n)$, а поиск максимального элемента — за $O(1)$. Например, после последовательного добавления `[10, 20, 5, 30]`, вызов `extractMax()` должен вернуть `30`, оставив кучу в сбалансированном состоянии.


### 4. Число инверсий в массиве

**Задача**  
Рассчитать число инверсий в массиве с использованием алгоритма "разделяй и властвуй" (например, модифицированного слияния) с временной сложностью $O(n\ \log\ n)$. Инверсией называется пара индексов $(i,\ j)$, где $i < j$ и $A[i] > A[j]$, то есть элементы идут в неправильном порядке относительно сортировки. 
Алгоритм "разделяй и властвуй" означает разбиение задачи на более мелкие подзадачи, их рекурсивное решение и объединение результатов. В данном случае массив рекурсивно разбивается на две половины, инверсии считаются в каждой половине отдельно, а затем корректируются при слиянии. 
Для эффективного подсчёта можно модифицировать алгоритм сортировки слиянием: при слиянии двух отсортированных подмассивов, если элемент из правой части меньше текущего элемента из левой, значит, он образует инверсии с оставшимися элементами левой части. Это позволяет учитывать инверсии за $O(n)$ на каждом уровне рекурсии, обеспечивая общую сложность $O(n\ \log\ n)$. Например, для массива `[2, 4, 1, 3, 5]` инверсии — `(2,1)`, `(4,1)`, `(4,3)`, дающие итоговое количество `3`.


### 5. Поиск количества покрытий точек отрезками

**Задача** 
Реализовать алгоритм поиска количества покрытий каждой точки отрезками, оптимизировав память и время за счёт сортировки на месте, элиминации хвостовой рекурсии и использования бинарного поиска. Даны $n$ отрезков $[l_i,\ r_i]$ и $m$ точек $p_j$. Необходимо для каждой точки определить, сколько отрезков её покрывают, то есть для каждого $p_j$ найти количество отрезков, удовлетворяющих $`l_i \leq p_j \leq r_i`$. Прямой перебор всех отрезков для каждой точки даёт сложность $O(n\ m)$, что неэффективно при больших входных данных. Для оптимизации используется метод событий: все начала и концы отрезков, а также точки заносятся в общий список событий, который затем сортируется. Сортировка на месте (in-place) позволяет не использовать дополнительную память для хранения отсортированных данных и выполняется с помощью стандартных алгоритмов сортировки, например, быстрой сортировки или сортировки слиянием. Элиминация хвостовой рекурсии применяется при обработке событий, чтобы уменьшить глубину рекурсии и избежать переполнения стека, заменяя рекурсивные вызовы на итеративные. Бинарный поиск используется для ускоренного определения числа активных отрезков для каждой точки, выполняя поиск по отсортированным границам отрезков. Таким образом, сначала сортируются все события, затем выполняется один проход по ним для обновления количества активных отрезков, а далее для каждой точки выполняется бинарный поиск, обеспечивая сложность $O(\ (n + m)\ \log\ (n + m)\ )$. Например, для отрезков  
`[ (1, 4), (2, 6), (8, 10) ]` и точек `[2, 5, 9]` результат будет `[2, 1, 1]` (точка `2` покрыта двумя отрезками, а `5` и `9` — одним).



### 6. Максимальная невозрастающая подпоследовательность

**Задача**  
Найти длину и индексы максимальной невозрастающей подпоследовательности массива. Дано массив $A$ из $n$ элементов. Требуется найти наиболее длинную подпоследовательность, в которой каждый следующий элемент не больше предыдущего. Индексы элементов в найденной подпоследовательности должны соответствовать их положению в исходном массиве. Например, для массива `[5, 3, 4, 4, 2]` одна из возможных максимальных невозрастающих подпоследовательностей — `[5, 4, 4, 2]` с индексами `[0, 2, 3, 4]`, а её длина — 4. Решение методом динамического программирования даёт сложность $O(n^2)$, но можно оптимизировать до $O(n\ \log\ n)$ с помощью бинарного поиска. Итоговое решение должно быть эффективно реализовано и возвращать не только длину, но и индексы найденной подпоследовательности.

### 7. Расстояние Левенштейна

**Задача:** Вычислить расстояние Левенштейна между двумя строками и вывести последовательность редакционных операций для преобразования одной строки в другую. Расстояние Левенштейна между строками $s$ и $t$ определяется как минимальное количество операций вставки, удаления или замены символов, необходимых для приведения $s$ к $t$. Динамическое программирование позволяет решить задачу за $O(n\ m)$, где $n$ и $m$ — длины строк. Для восстановления оптимальной последовательности операций используется обратный проход по матрице расстояний, что позволяет получить шаги преобразования. Например, для строк `kitten` и `sitting` расстояние Левенштейна равно 3, а последовательность операций: `k -> s` (замена), `e -> i` (замена), добавление `g` в конец.

### 8. Максимальная сумма на лестнице

**Задача**  
Найти максимальную сумму чисел, отмеченных на ступеньках, при подъёме по лестнице с $n$ ступенями, переходя на одну или две ступеньки за раз. Дано $n$ ступенек, на каждой из которых написано некоторое число. Начинается подъём с первой ступеньки, и за один шаг можно перейти либо на следующую, либо через одну. Необходимо найти такой путь, при котором сумма чисел на ступеньках будет максимальной. Оптимальное решение строится с помощью динамического программирования за $O(n)$, где $dp[i]$ — максимальная сумма, достигаемая на $i$-й ступеньке. Например, для лестницы `[2, -1, 4, 5, -2]` оптимальный путь — `2 -> 4 -> 5`, сумма `11`.

### 9. Собственная реализация списка (List)

**Задача**  
Реализовать собственную версию динамического списка, аналогичного встроенному `Array` в JavaScript, без использования массивов и встроенных коллекций. Должны поддерживаться основные операции: добавление элемента в конец, получение элемента по индексу, удаление элемента, изменение размера списка и получение текущей длины. Внутренняя реализация должна использовать связанные структуры данных, например, односвязный или двусвязный список. Операции должны быть реализованы с учётом эффективности: добавление в конец — $O(1)$, доступ по индексу — $O(n)$ (для связного списка) или $O(1)$ (для альтернативной реализации с массивоподобной структурой на основе объектов). Например, `const list = new CustomList(); list.add(10); list.add(20); console.log(list.get(1)); // 20`


### 10. Реализация структур данных (Deque, List, PriorityQueue) 🏗️

**Задача**  
Реализовать классы для структур данных в JavaScript без использования встроенных массивов и коллекций:
- **Deque** — двусторонняя очередь (Double-Ended Queue) с возможностью добавления и удаления элементов с обоих концов,
- **LinkedList** — двусвязный список (Doubly Linked List) с доступом к первому и последнему элементу,
- **PriorityQueue** — приоритетная очередь (Priority Queue) на основе кучи (Heap) с возможностью добавления элементов с приоритетом и извлечения максимального (или минимального) элемента.

Каждая структура должна поддерживать базовые операции, реализуемые через методы:

| Метод | Описание |
|---------------------|---------------------------|
| `size()` | Возвращает количество элементов в структуре. |
| `clear()` | Очищает структуру, удаляя все элементы. |
| `add(element)` | Добавляет элемент в структуру. |
| `remove()` | Удаляет и возвращает элемент из структуры (для очередей и стеков). |
| `contains(element)` | Проверяет, содержится ли элемент в структуре. |
| `offer(element)` | Добавляет элемент с приоритетом (для PriorityQueue). |
| `poll()` | Извлекает и удаляет элемент с наивысшим приоритетом. |
| `peek()` | Возвращает элемент с наивысшим приоритетом без удаления. |
| `element()` | Возвращает текущий элемент (аналог `peek()`, но выбрасывает ошибку, если пусто). |
| `isEmpty()` | Проверяет, пустая ли структура. |
| `containsAll(collection)` | Проверяет, содержатся ли все элементы из другой коллекции. |
| `addAll(collection)` | Добавляет все элементы из другой коллекции. |
| `removeAll(collection)` | Удаляет все элементы, содержащиеся в другой коллекции. |
| `retainAll(collection)` | Удаляет все элементы, кроме тех, которые есть в другой коллекции. |
| `removeAt(index)` | Удаляет элемент по индексу. |
| `removeElement(element)` | Удаляет конкретный элемент. |
| `addFirst(element)` | Добавляет элемент в начало (для Deque). |
| `addLast(element)` | Добавляет элемент в конец (для Deque). |
| `getFirst()` | Возвращает первый элемент (для Deque). |
| `getLast()` | Возвращает последний элемент (для Deque). |
| `pollFirst()` | Удаляет и возвращает первый элемент (для Deque). |
| `pollLast()` | Удаляет и возвращает последний элемент (для Deque). |

Deque должен обеспечивать добавление и удаление элементов с обоих концов за $O(1)$, двусвязный список — эффективное добавление и удаление за $O(1)$, а приоритетная очередь должна работать на основе двоичной кучи с $O(\log\ n)$ для операций добавления и удаления. Например, `const pq = new PriorityQueue(); pq.offer(5); pq.offer(2); console.log(pq.poll()); // 5`



### 11. Реализация множества (Set) 🔗
**Задача:** Реализовать классы для структур данных:
- **A** — хеш-ножницы (`HashSet`) на основе массива и односвязного списка для коллизий,
- **B** — связанный хеш-сет (`LinkedHashSet`),
- **C** — дерево-сет (`TreeSet`) на основе отсортированного массива,
- Без использования стандартных классов библиотеки.

| Методы к реализации |
|---------------------|
| ```size(), clear(), isEmpty(), add(Object), remove(Object), contains(Object), toString(), containsAll(Collection), addAll(Collection), removeAll(Collection), retainAll(Collection)``` |

### 12. Реализация Map на деревьях 🌳
**Задача:** Создать классы, реализующие интерфейс `Map` с использованием различных типов деревьев:
- **A** — АВЛ-дерево,
- **B** — красно-черное дерево,
- **C** — splay-дерево.

Методы `toString()` должны выводить элементы в порядке возрастания ключей.

| Методы к реализации |
|---------------------|
| ```toString(), put(Integer, String), remove(Integer), get(Integer), containsKey(Integer), containsValue(String), size(), clear(), isEmpty(), headMap(Integer), tailMap(Integer), firstKey(), lastKey(), lowerKey(Integer), floorKey(Integer), ceilingKey(Integer), higherKey(Integer)``` |

### 13. Топологическая сортировка и связность в орграфах 🔀
**Задача:** Реализовать алгоритмы для:
- Топологической сортировки,
- Проверки циклов,
- Нахождения компонент сильной связности в орграфах.

### 14. Кластеризация и DSU 🧩
**Задача:** Реализовать решения для:
- Кластеризации точек и сайтов с использованием структуры данных `DSU` (Disjoint Set Union),
- Решения задачи Ханойских башен с помощью `DSU` для отслеживания поддеревьев.

### 15. Обработка Java-файлов 📂
**Задача:** Написать программу, которая обрабатывает файлы `.java` в каталоге `src` и его подкаталогах, игнорируя тесты (`@Test` или `org.junit.Test`).

#### Требования:
- Удалить строку с пакетом и импорты,
- Удалить комментарии и символы с кодом `<33`,
- Рассчитать размер обработанного текста,
- Отсортировать файлы по размеру и пути,
- Найти пары файлов с похожими текстами по метрике Левенштейна (менее 10 правок),
- Обеспечить обработку ошибок,
- Не вносить изменения на диск.
